# Python testing: pytest

This is a simple Python project used to practice writing tests with pytest.

## Requirements

- Python 3.9+

## Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
```

## Running tests

From the project root:

```bash
pytest -q
```
